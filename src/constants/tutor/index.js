const SUBJECTS = require("./subject");
const OCCUPATION_STATUS = require("./occupationStatus");
const { TUTOR_STATUS, DAYS_OF_WEEK, PHONE_REGEX, TIME_REGEX } = require("./tutor");

module.exports = {
  SUBJECTS,
  OCCUPATION_STATUS,
  TUTOR_STATUS,
  DAYS_OF_WEEK,
  PHONE_REGEX,
  TIME_REGEX,
};
